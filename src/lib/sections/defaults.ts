/**
 * The places the "Discover India" section showed before anyone chose their own.
 * Used by the homepage while a section still says "discover", and to prefill the
 * editor so the first save keeps the page exactly as it was.
 */
export const DISCOVER_DEFAULT_SLUGS = ["jaisalmer", "goa", "kashmir", "leh", "udaipur", "varanasi"];

/** The editorial destination layout: one large photograph and four smaller. */
export const DESTINATION_SLOTS = 5;
